[project]
name = "photo-upload-worker"
version = "0.1.0"
description = "Python Worker with FastAPI for photo uploads to R2"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "python-multipart",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk"
]
